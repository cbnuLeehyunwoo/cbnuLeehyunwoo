name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *" # 매일 자정마다 실행
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: cbnuLeehyunwoo
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ github.token }}  # github.token 사용

      - name: Debug Git Info
        run: |
          git config --list
          git remote -v
          git branch -a
          git status
        env:
          GITHUB_TOKEN: ${{ github.token }}  # github.token 사용
